/*
Atribuição composta com multiplicação aumentada
O *=operador multiplica uma variável por um número.

myVar = myVar * 5;
irá multiplicar myVarpor 5. Isso pode ser reescrito como:

myVar *= 5;
Converter as atribuições para a, be cusar o *=operador.
*/

var a = 5;
var b = 12;
var c = 4.6;

// Only change code below this line
a *= 5;
b *= 3;
c *= 10;

/*
a deve ser igual 25.

b deve ser igual 36.

c deve ser igual 46.

Você deve usar o *= operador para cada variável.

Você não deve modificar o código acima do comentário especificado.
*/
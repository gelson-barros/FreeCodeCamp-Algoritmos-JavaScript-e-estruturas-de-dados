/*
Armazenando Valores com o Operador de Atribuição
Em JavaScript, você pode armazenar um valor em uma variável com o operador de atribuição ( =).

myVariable = 5;
Isso atribui o Numbervalor 5a myVariable.

Se houver cálculos à direita do =operador, eles serão executados antes de o valor ser atribuído à variável à esquerda do operador.

var myVar;
myVar = 5;
Primeiro, este código cria uma variável chamada myVar. Em seguida, o código atribui 5a myVar. Agora, se myVaraparecer novamente no código, o programa o tratará como se fosse 5.

Atribua o valor 7 à variável a.
*/

// Setup
var a;

// Only change code below this line
a = 7;

/*
Você não deve alterar o código acima do comentário especificado.

a deve ter um valor de 7.
*/
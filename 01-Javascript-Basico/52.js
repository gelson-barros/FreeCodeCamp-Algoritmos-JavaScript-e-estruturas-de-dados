/*
Retorne um valor de uma função com retorno

Podemos passar valores para uma função com argumentos. Você pode usar uma return instrução para enviar um valor de volta de uma função.

Exemplo

function plusThree(num) {
  return num + 3;
}
var answer = plusThree(5);
answertem o valor 8.

plusThreerecebe um argumento para nume retorna um valor igual a num + 3.

Crie uma função timesFiveque aceite um argumento, multiplique-o por 5e retorne o novo valor.
*/

function timesFive(a){
  return a*5;
}

/*
timesFive deve ser uma função

timesFive(5) deveria retornar 25

timesFive(2) deveria retornar 10

timesFive(0) deveria retornar 0
*/
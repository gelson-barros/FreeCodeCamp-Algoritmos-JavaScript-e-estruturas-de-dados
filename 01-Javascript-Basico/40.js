/*
Modificar dados de matriz com índices

Ao contrário das strings, as entradas dos arrays são mutáveis e podem ser alteradas livremente.

Exemplo

var ourArray = [50,40,30];
ourArray[0] = 15;
ourArray agora tem o valor [15, 40, 30].

Nota: Não deve haver nenhum espaço entre o nome da matriz e os colchetes, como array [0]. Embora o JavaScript seja capaz de processar isso corretamente, isso pode confundir outros programadores que estejam lendo seu código.

Modifique os dados armazenados no índice 0de myArray para um valor de 45.
*/

// Setup
var myArray = [18,64,99];

// Only change code below this line
myArray[0] = 45;
/*
myArray agora deve ser [45,64,99].

Você deve usar o índice correto para modificar o valor em myArray.
*/
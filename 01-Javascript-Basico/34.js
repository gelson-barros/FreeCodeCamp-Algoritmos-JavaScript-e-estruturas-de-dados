/*
Use a notação de colchetes para encontrar o último caractere em uma string
Para obter a última letra de uma string, você pode subtrair uma do comprimento da string.

Por exemplo, se var firstName = "Ada", você pode obter o valor da última letra da string usando firstName[firstName.length - 1].

Exemplo:

var firstName = "Ada";
var lastLetter = firstName[firstName.length - 1];
lastLetterteria um valor da string a.

Use a notação de colchetes para encontrar o último caractere na lastNamevariável.

Dica: tente olhar o exemplo acima se você ficar preso.
*/

// Setup
var lastName = "Lovelace";

// Only change code below this line
var lastLetterOfLastName = lastName[lastName.length - 1]; // Change this line

/*
lastLetterOfLastName deve ser a carta e.

Você deve usar .length para obter a última letra.
*/
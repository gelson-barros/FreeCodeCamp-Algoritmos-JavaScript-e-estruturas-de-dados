/*
Declarar variáveis ​​JavaScript
Na ciência da computação, dados são qualquer coisa significativa para o computador. JavaScript fornece oito diferentes tipos de dados que são undefined, null, boolean, string, symbol, bigint, number, e object.

Por exemplo, computadores distinguir entre números, tais como o número 12 e strings, tais como "12", "dog", ou "123 cats", que são conjuntos de caracteres. Os computadores podem realizar operações matemáticas em um número, mas não em uma string.

As variáveis permitem que os computadores armazenem e manipulem dados de maneira dinâmica. Eles fazem isso usando um "rótulo" para apontar para os dados em vez de usar os próprios dados. Qualquer um dos oito tipos de dados pode ser armazenado em uma variável.

As variáveis ​​são semelhantes às variáveis ​​xey que você usa em matemática, o que significa que são um nome simples para representar os dados aos quais queremos nos referir. Variáveis ​​de computador diferem de variáveis ​​matemáticas porque podem armazenar valores diferentes em momentos diferentes.

Dizemos ao JavaScript para criar ou declarar uma variável colocando a palavra-chave varna frente dela, assim:

var ourName;

cria uma variável chamada ourName. Em JavaScript, terminamos as declarações com ponto e vírgula. Os nomes das variáveis ​​podem ser compostos por números, letras e $ou _, mas não podem conter espaços ou começar com um número.

Use a var palavra-chave para criar uma variável chamada myName.

Dica
Veja o ourName exemplo acima se tiver dúvidas.

Você deve declarar myName com a var palavra - chave, terminando com um ponto e vírgula
*/
var myName;

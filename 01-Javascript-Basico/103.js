/*
Gerar frações aleatórias com JavaScriptPassado
Os números aleatórios são úteis para criar um comportamento aleatório.

JavaScript tem uma Math.random()função que gera um número decimal aleatório entre 0(inclusivo) e 1(exclusivo). Portanto, Math.random()pode retornar a, 0mas nunca retornar a 1.

Observação: como no armazenamento de valores com o operador de atribuição , todas as chamadas de função serão resolvidas antes da returnexecução, para que possamos determinar returno valor da Math.random()função.

Altere randomFractionpara retornar um número aleatório em vez de retornar 0.

*/

function randomFraction() {
    // Only change code below this line

    return 0;

    // Only change code above this line
}

/*
randomFraction deve retornar um número aleatório.

O número retornado por randomFractiondeve ser um decimal.

Você deve usar Math.randompara gerar o número decimal aleatório.
*/
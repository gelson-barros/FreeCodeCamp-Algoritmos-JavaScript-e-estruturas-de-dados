/*
Iterar com JavaScript While LoopsPassado
Você pode executar o mesmo código várias vezes usando um loop.

O primeiro tipo de loop que aprenderemos é chamado de whileloop porque é executado enquanto uma condição especificada é verdadeira e para quando essa condição não é mais verdadeira.

var ourArray = [];
var i = 0;
while(i < 5) {
  ourArray.push(i);
  i++;
}
No exemplo de código acima, o whileloop será executado 5 vezes e acrescentará os números de 0 a 4 a ourArray.

Vamos tentar fazer com que um loop while funcione enviando valores para um array.

Adicione os números de 5 a 0 (inclusive) em ordem decrescente para myArrayusar um whileloop.
*/

// Setup
var myArray = [];

// Only change code below this line

/*
Você deve usar um whileloop para isso.

myArraydeve ser igual [5,4,3,2,1,0].
*/
/*
Chaining If Else StatementsPassado
if/elsedeclarações podem ser encadeadas para lógica complexa. Aqui está o pseudocódigo de várias instruções if/ encadeadas else if:

if (condition1) {
  statement1
} else if (condition2) {
  statement2
} else if (condition3) {
  statement3
. . .
} else {
  statementN
}
Escreva declarações if/ encadeadas else ifpara cumprir as seguintes condições:

num < 5- return Tiny
num < 10- return Small
num < 15- return Medium
num < 20- return Large
num >= 20- returnHuge
*/

function testSize(num) {
    // Only change code below this line

    return "Change Me";
    // Only change code above this line
}

testSize(7);

/*
Você deve ter pelo menos quatro elsedeclarações

Você deve ter pelo menos quatro ifdeclarações

Você deve ter pelo menos uma returndeclaração

testSize(0) deve retornar a string Tiny

testSize(4) deve retornar a string Tiny

testSize(5) deve retornar a string Small

testSize(8) deve retornar a string Small

testSize(10) deve retornar a string Medium

testSize(14) deve retornar a string Medium

testSize(15) deve retornar a string Large

testSize(17) deve retornar a string Large

testSize(20) deve retornar a string Huge

testSize(25) deve retornar a string Huge
*/
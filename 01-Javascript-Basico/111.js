/*
Use a recursão para criar um intervalo de númerosPassado
Continuando com o desafio anterior, oferecemos a você outra oportunidade de criar uma função recursiva para resolver um problema.

Definimos uma função nomeada rangeOfNumberscom dois parâmetros. A função deve retornar uma matriz de inteiros que começa com um número representado pelo startNumparâmetro e termina com um número representado pelo endNumparâmetro. O número inicial será sempre menor ou igual ao número final. Sua função deve usar recursão chamando a si mesma e não usar loops de nenhum tipo. Também deve funcionar para casos em que startNume endNumsão iguais.
*/

function rangeOfNumbers(startNum, endNum) {
  return [];
};

/*
Sua função deve retornar um array.

Seu código não deve usar qualquer sintaxe loop ( forou whileou mais funções de ordem tais como forEach, map, filter, ou reduce).

rangeOfNumbers deve usar recursão (chamar a si mesmo) para resolver esse desafio.

rangeOfNumbers(1, 5)deve retornar [1, 2, 3, 4, 5].

rangeOfNumbers(6, 9)deve retornar [6, 7, 8, 9].

rangeOfNumbers(4, 4)deve retornar [4].
*/
/*
Atribuindo o valor de uma variável a outra 
Depois que um valor é atribuído a uma variável usando o operador de atribuição , você pode atribuir o valor dessa variável a outra variável usando o operador de atribuição .

var myVar;
myVar = 5;
var myNum;
myNum = myVar;
O código acima declara uma myVarvariável sem valor e, a seguir, atribui o valor a ela 5. Em seguida, uma variável chamada myNumé declarada sem valor. Em seguida, o conteúdo de myVar(que é 5) é atribuído à variável myNum. Agora, myNumtambém tem o valor de 5.

Atribua o conteúdo de aà variável b.

*/

// Setup
var a;
a = 7;
var b;

// Only change code below this line
b = a;

/*
Você não deve alterar o código acima do comentário especificado.

b deve ter um valor de 7.

a deve ser atribuído a b com =.
*/
/*
Seleção de muitas opções com declarações de switchPassado
Se você tiver muitas opções para escolher, use uma instrução switch . Uma switchdeclaração testa um valor e pode ter muitas declarações de caso que definem vários valores possíveis. As instruções são executadas a partir do primeiro casevalor correspondido até que um breakseja encontrado.

Aqui está um exemplo de uma switchdeclaração:

switch(lowercaseLetter) {
  case "a":
    console.log("A");
    break;
  case "b":
    console.log("B");
    break;
}
caseos valores são testados com igualdade estrita ( ===). O breakdiz ao JavaScript para parar de executar instruções. Se breakfor omitido, a próxima instrução será executada.

Escreva uma instrução switch que testa vale define answeras seguintes condições:
1- alpha
2- beta
3- gamma
4-delta
*/

function caseInSwitch(val) {
    var answer = "";
    // Only change code below this line

    // Only change code above this line
    return answer;
}

caseInSwitch(1);

/*
caseInSwitch(1) deve ter um valor da string alpha

caseInSwitch(2) deve ter um valor da string beta

caseInSwitch(3) deve ter um valor da string gamma

caseInSwitch(4) deve ter um valor da string delta

Você não deve usar nenhuma declaração ifouelse

Você deve ter pelo menos 3 breakdeclarações
*/
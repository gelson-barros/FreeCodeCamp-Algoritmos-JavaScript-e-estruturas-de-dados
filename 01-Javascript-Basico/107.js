/*
Use a função parseInt com um RadixPassado
A parseInt()função analisa uma string e retorna um inteiro. Leva um segundo argumento para a raiz, que especifica a base do número na string. A raiz pode ser um número inteiro entre 2 e 36.

A chamada de função se parece com:

parseInt(string, radix);
E aqui está um exemplo:

var a = parseInt("11", 2);
A variável raiz diz que 11está no sistema binário, ou base 2. Este exemplo converte a string 11em um inteiro 3.

Use parseInt()na convertToIntegerfunção para que ela converta um número binário em um inteiro e o retorne.
*/

function convertToInteger(str) {

}

convertToInteger("10011");


/*
convertToIntegerdeve usar a parseInt()função

convertToInteger("10011") deve retornar um número

convertToInteger("10011") deve retornar 19

convertToInteger("111001") deve retornar 57

convertToInteger("JamesBond") deveria retornar NaN
*/
/*
Gerar números inteiros aleatórios dentro de um intervaloPassado
Em vez de gerar um número inteiro aleatório entre zero e um determinado número como fizemos antes, podemos gerar um número inteiro aleatório que se enquadre em um intervalo de dois números específicos.

Para fazer isso, vamos definir um número mínimo mine um número máximo max.

Aqui está a fórmula que usaremos. Reserve um momento para lê-lo e tente entender o que este código está fazendo:

Math.floor(Math.random() * (max - min + 1)) + min
Crie uma função chamada randomRangeque receba um intervalo myMine myMaxretorne um número inteiro aleatório maior ou igual a myMine menor ou igual a myMaxinclusive.
*/

function randomRange(myMin, myMax) {
    // Only change code below this line
    return 0;
    // Only change code above this line
}

/*
O menor número aleatório que pode ser gerado por randomRangedeve ser igual ao seu número mínimo myMin,.

O maior número aleatório que pode ser gerado por randomRangedeve ser igual ao seu número máximo myMax,.

O número aleatório gerado por randomRangedeve ser um número inteiro, não decimal.

randomRangedeve usar myMaxe myMin, e retornar um número aleatório em seu intervalo.
*/

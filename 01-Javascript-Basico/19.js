/*
Atribuição composta com subtração aumentadaPassado
Como o +=operador, -=subtrai um número de uma variável.

myVar = myVar - 5;
irá subtrair 5de myVar. Isso pode ser reescrito como:

myVar -= 5;
Converter as atribuições para a, be cusar o -=operador.
*/

var a = 11;
var b = 9;
var c = 3;

// Only change code below this line
a -= 6;
b -= 15;
c -= 1;

/*
a deve ser igual 5.

b deve ser igual -6.

c deve ser igual 2.

Você deve usar o -= operador para cada variável.

Você não deve modificar o código acima do comentário especificado.
*/
/*
Compreendendo os valores booleanos

Outro tipo de dado é o Booleano . Os booleanos podem ter apenas um de dois valores: true ou false. Eles são basicamente pequenos interruptores liga-desliga, onde true está ligado e false desligado. Esses dois estados são mutuamente exclusivos.

Observação: os valores booleanos nunca são escritos com aspas. As strings "true" e "false" não são booleanas e não têm nenhum significado especial em JavaScript.

Modifique a welcomeToBooleans função para que ela retorne em true vez de false quando o botão Executar for clicado.
*/

function welcomeToBooleans() {
    // Only change code below this line

    return true; // Change this line

    // Only change code above this line
}

/*

A welcomeToBooleans()função deve retornar um valor booleano ( true ou false).

welcomeToBooleans()deve retornar true.
*/
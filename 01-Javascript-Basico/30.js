/*
Encontre o comprimento de uma corda
Você pode encontrar o comprimento de um String valor escrevendo .length após a variável de string ou literal de string.

console.log("Alan Peter".length);
O valor 10 seria exibido no console.

Por exemplo, se criarmos uma variável var firstName = "Ada", poderíamos descobrir o comprimento da string Ada usando a firstName.length propriedade.

Use a .length propriedade para contar o número de caracteres na lastNamevariável e atribuí-la a lastNameLength.
*/

// Setup
var lastNameLength = 0;
var lastName = "Lovelace";

// Only change code below this line

lastNameLength = lastName.length;

/*
Você não deve alterar as declarações de variáveis ​​na // Setup seção.

lastNameLength deve ser igual a oito.

Você deve estar recebendo o comprimento lastName usando .lengthcomo este: lastName.length.
*/
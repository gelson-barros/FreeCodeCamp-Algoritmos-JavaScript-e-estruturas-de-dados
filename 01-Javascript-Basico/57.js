/*
Use lógica condicional com instruções If

If declarações são usadas para tomar decisões no código. A palavra-chave if diz ao JavaScript para executar o código entre chaves sob certas condições, definidas entre parênteses. Essas condições são conhecidas como Boolean condições e podem ser apenas tru e ou false.

Quando a condição é avaliada como true, o programa executa a instrução dentro das chaves. Quando a condição booleana for avaliada como false, a instrução dentro das chaves não será executada.

Pseudo-código

if ( condição é verdadeira ) {
  instrução é executada
}
Exemplo

function test (myCondition) {
  if (myCondition) {
     return "It was true";
  }
  return "It was false";
}
test(true);
test(false);
test(true)retorna a string It was truee test(false)retorna a string It was false.

Quando testé chamado com um valor de true, a ifinstrução é avaliada myConditionpara ver se é trueou não. Sendo assim true, a função retorna It was true. Quando chamamos testcom um valor de false, nãomyCondition é e a instrução entre chaves não é executada e a função retorna . trueIt was false

Crie uma ifinstrução dentro da função para retornar Yes, that was truese o parâmetro wasThatTruefor truee retornar No, that was falsecaso contrário.
*/

function trueOrFalse(wasThatTrue) {
    // Only change code below this line

    // Only change code above this line
}

/*
trueOrFalse deve ser uma função

trueOrFalse(true) deve retornar uma string

trueOrFalse(false) deve retornar uma string

trueOrFalse(true) deve retornar a string Yes, that was true

trueOrFalse(false) deve retornar a string No, that was false
*/
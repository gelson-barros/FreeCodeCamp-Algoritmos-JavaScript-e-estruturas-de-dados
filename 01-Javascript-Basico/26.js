/*
Concatenando Strings com o Operador Plus
Em JavaScript, quando o +operador é usado com um Stringvalor, ele é chamado de operador de concatenação . Você pode construir uma nova string a partir de outras strings concatenando -as.

Exemplo

'My name is Alan,' + ' I concatenate.'
Nota: Cuidado com os espaços. A concatenação não adiciona espaços entre strings concatenadas, portanto, você mesmo precisará adicioná-los.

Exemplo:

var ourStr = "I come first. " + "I come second.";
A string I come first. I come second.seria exibida no console.

Construa a myStr partir das strings This is the start. e This is the end. usando o + operador.
*/

var myStr = "This is the start" + "This is the end"; // Change this line

/*
myStr deve ter um valor da string This is the start. This is the end.

Você deve usar o + operador para construir myStr.

myStr deve ser criado usando a var palavra - chave.

Você deve atribuir o resultado à myStrvariável.
*/